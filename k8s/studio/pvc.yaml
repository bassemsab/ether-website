apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agent-runner-pvc
  namespace: ether
  labels:
    app: agent-runner
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
